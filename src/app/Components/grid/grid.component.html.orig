
  <nb-card>
      <nb-card-header>
        {{titulo}}
        <button (click)="Sayhello()">asass</button>
      </nb-card-header>
      <nb-card-body>
        <ng2-smart-table [settings]="settings" [source]="menuItems">
        </ng2-smart-table>
      </nb-card-body>
    </nb-card>